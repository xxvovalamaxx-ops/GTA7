# GTA7 TODO

- [ ] Identify engine/language (run file‑type search).
- [ ] Install required engine version and SDKs.
- [ ] Scaffold project directories (`Assets/`, `Scripts/`, `Docs/`).
- [ ] Add a `.gitignore` appropriate for the chosen engine.
- [ ] Create a minimal scene/prototype (e.g., placeholder map, vehicle placeholder).
- [ ] Add placeholder assets (low‑poly models, textures) – you can use Blender for modelling.
- [ ] Write build scripts (`build.bat` / `build.sh`).
- [ ] Set up Git LFS for large binary assets.
- [ ] Configure a CI pipeline (GitHub Actions) to build and run tests.
- [ ] Draft design documentation (`Docs/Design.md`).
- [ ] Set up daily notes and progress logs (already in place).

*Mark tasks as completed as you finish them.*